# FUTURE

Ideas not required for immediate support:

- Phase 0: add a non-forking build verification that compiles the `m80` executable (beyond build script configuration).
- Phase 0: add an end-to-end CLI help path test that exercises `main()` argument parsing without spawning a subprocess.
